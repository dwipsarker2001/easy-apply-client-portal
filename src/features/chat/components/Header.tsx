import React from "react";

const Header: React.FC = () => {
  return (
    <header className="h-[100px] bg-green-200 flex items-center justify-center">
      Chat Page
    </header>
  );
};

export default Header;
