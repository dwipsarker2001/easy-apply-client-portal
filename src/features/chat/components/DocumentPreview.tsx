interface DocumentPreviewProps {
  file: File;
  type: '.pdf';
}
const DocumentPreview = ({ file, type }: DocumentPreviewProps) => {
  return <div>DocumentPreview</div>;
};

export default DocumentPreview;
